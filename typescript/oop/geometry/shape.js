class Shape {
  constructor(color, filled) {
    this.color = color;
    this.filled = filled;
  }
  setColor(newColor) {
    this.color = newColor;
  }
  filled(fill) {
    this.filled = fill;
  }
}
